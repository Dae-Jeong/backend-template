# 이 파일을 .env로 복사하고 필요한 값만 변경합니다.
# 프로세스 환경변수 > .env > Settings 기본값 순서로 적용합니다.
APP_NAME=Backend Template
SERVICE_VERSION=0.1.0
SERVER_HOST=127.0.0.1
SERVER_PORT=18080
# debug / info / warning / error / critical
LOG_LEVEL=info
